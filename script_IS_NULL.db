SELECT COUNT(*) AS TOTAL_LINHAS_VAZIAS
        
FROM RESULTADOS R
WHERE R.NM_MUNICIPIO IS NULL 
    OR R.NR_TURNO IS NULL
    OR R.NR_ZONA IS NULL
    OR R.NR_SECAO IS NULL
    OR R.DS_CARGO IS NULL
    OR R.NM_VOTAVEL IS NULL
    OR R.QT_VOTOS IS NULL
    OR R.NM_LOCAL_VOTACAO IS NULL